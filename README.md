# Educonnect-Ruben

Plataforma educativa moderna.

- **Frontend**: HTML/CSS/JS Vainilla (Pro UI).
- **Backend**: FastAPI (Python), PostgreSQL.

## Despliegue en Render
El proyecto está configurado para desplegarse automáticamente usando `render.yaml`.
